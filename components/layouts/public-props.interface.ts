export default interface PublicProps {
    variant?: 'without-steps' | 'with-steps'
    heading?: React.ReactNode
    logo?: React.ReactNode
    bottom?: React.ReactNode
    children?: React.ReactNode
}
